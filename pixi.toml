[workspace]
authors = ["Vincent Leroy <vincent.leroy@rayference.eu>"]
channels = ["conda-forge"]
description = "Vincent's personal website"
name = "leroyvn-github-io"
platforms = ["linux-64", "osx-arm64"]
version = "0.1.0"

[tasks]
build = "hugo --minify"
clean = "rm -rf build"
server = "hugo server"

[dependencies]
python = "3.14"
pip = "*"

[pypi-dependencies]
hugo = ">=0.164, <0.165"
